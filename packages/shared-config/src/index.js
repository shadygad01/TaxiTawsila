// CommonJS entry point kept for tooling that resolves `main` (e.g. package
// managers, IDE integrations). The actual ESLint preset is ESM — see
// eslint-preset.mjs — imported directly by consumers rather than through here.
module.exports = {};
