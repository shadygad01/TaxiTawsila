{
  "name": "@taxitawsila/shared-config",
  "version": "0.0.0",
  "private": true,
  "description": "Shared ESLint/TypeScript/Prettier base configuration (Folder Structure §5). Single source of truth — apps extend these rather than redefining rules.",
  "main": "src/index.js",
  "files": ["src", "tsconfig.base.json", "prettier-preset.json"],
  "scripts": {
    "lint": "echo 'shared-config has no lintable source of its own'",
    "typecheck": "echo 'shared-config has no TS source to typecheck'",
    "test": "echo 'shared-config has no tests of its own'"
  },
  "dependencies": {
    "@eslint/js": "^9.15.0",
    "typescript-eslint": "^8.15.0",
    "eslint-config-prettier": "^9.1.0"
  }
}
