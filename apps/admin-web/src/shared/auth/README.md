# Admin Session / RBAC Guard

Deferred — no admin authentication flow exists yet (Identity Context is a Phase 2 stub, see apps/backend/src/modules/identity/README.md). This directory will hold the admin session/RBAC guard once Identity's admin-auth business logic exists to call.
