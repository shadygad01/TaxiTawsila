# Configuration

Deferred — Phase 2 excludes this feature's backend business logic (docs/21-phase2-foundation-audit.md), so there is nothing yet for this admin UI to call. Folder structure (`api/`, `components/`, `pages/`, `hooks/`) is scaffolded per Folder Structure §4, ready for the phase that implements it.
