name: taxitawsila_mobile
description: >
  Taxi Alexandria Platform passenger app (Flutter Clean Architecture,
  Folder Structure §3). Phase 2 scope: folder scaffolding only — see
  docs/21-phase2-foundation-audit.md. No Trip/Fare/Maps/GPS/Rewards/Ads/Auth
  business logic is implemented.
publish_to: "none"
version: 0.0.0

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
