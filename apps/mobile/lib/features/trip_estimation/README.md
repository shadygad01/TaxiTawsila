# TripEstimation

Trip Context — fare estimation UI. Excluded from Phase 2 (Trip, Fare estimation). Folder structure (`domain/`, `data/`, `presentation/`) scaffolded per Folder Structure §3, empty until its owning phase (docs/15-roadmap.md) implements it.
